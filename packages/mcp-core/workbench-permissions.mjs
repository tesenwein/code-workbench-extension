// ESM re-export shim for workbench-permissions.cjs — the single canonical
// MCP permission allowlist. Keep this file plain ESM with no non-builtin
// imports.

export { WORKBENCH_PERMISSIONS } from "./workbench-permissions.cjs";
