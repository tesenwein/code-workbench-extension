"use strict";

// Generated from the shared phase procedures so the skill and the prompt the
// Phase Board spawns for this phase are always the same text.
const { phaseSkill } = require("../phase-prompts.cjs");

module.exports = phaseSkill("implement");
