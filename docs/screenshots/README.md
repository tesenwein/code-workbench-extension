# Screenshots

Drop PNGs here with these exact filenames so the root `README.md` renders them:

| File           | What to capture                                                                        |
| -------------- | -------------------------------------------------------------------------------------- |
| `overview.png` | Full workbench — sidebar (repos/worktrees), a Claude session, editor, and diff visible |
| `sessions.png` | Claude / Yolo / shell terminal tabs, color-tagged per worktree                         |
| `git.png`      | Git status, branch graph, and a side-by-side diff                                      |
| `tasks.png`    | The task board with a few tasks                                                        |

Aim for ~820px-wide captures (the README sizes them to 820). A real repo with a couple
of worktrees and an active session looks best.
